# MP4
"""
MP4 逐字稿轉寫工具（Tkinter GUI）

1. 功能：在本機把 MP4 抽出音訊、切成多段 WAV，透過 Faster-Whisper 模型進行語音轉文字，輸出純文字逐字稿。 
2. 依賴：需要 ffmpeg、pydub、faster-whisper 等套件與已下載的 Whisper 模型權重（預設 large-v3）。 
3. 介面：提供檔案選擇、切段長度、模型與語言選擇、VAD 靜默長度、Beam size 等參數調整，並以進度列與日誌顯示處理狀態。 
4. 輸出：在程式所在目錄下建立 transcripts 資料夾，將完成的逐字稿以 UTF-8 編碼 .txt 檔形式輸出，不會自動上傳到網路。 
5. 隱私：所有影音與文字處理皆在本機進行；請自行妥善保管輸出的 WAV/文字檔，避免含有敏感內容的檔案被未授權者存取。
"""
